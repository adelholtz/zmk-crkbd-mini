description: Adelholtz custom behavior for process_record_user

compatible: "zmk,apr"

include: [behaviors/behavior.yaml]

properties:
  "#binding-cells":
    type: int
    required: true
    const: 1
